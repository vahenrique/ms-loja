server:
  port: 8083

spring:
  application:
    name: TRANSPORTADORA-SERVICE
  datasource: 
    url: 'jdbc:mysql://localhost/transportadora?createDatabaseIfNotExist=true&serverTimezone=UTC'
    username: vinicius
    password: 'vinicius_123A'
  jpa:
    properties:
      hibernate:
        '[show_sql]': true
        '[format_sql]': true